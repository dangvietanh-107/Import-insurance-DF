import{OUTPUT_COLUMNS}from'./defaults.js';export function buildOutputFileName(name){const base=name.replace(/\.[^.]+$/,'');return`${base.replace(/input$/i,'output')===base?`${base}_output`:base.replace(/input$/i,'output')}.xlsx`}export function downloadWorkbook(rows,name){const ws=XLSX.utils.json_to_sheet(rows,{header:OUTPUT_COLUMNS,skipHeader:false});OUTPUT_COLUMNS.forEach((_,index)=>{const cell=XLSX.utils.encode_cell({r:0,c:index});ws[cell].s={font:{bold:true}}});ws['!cols']=OUTPUT_COLUMNS.map(c=>({wch:Math.max(12,Math.min(28,c.length+2))}));const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,ws,'Output');XLSX.writeFile(wb,buildOutputFileName(name),{compression:true})}
